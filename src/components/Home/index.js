const Home = () => {
	return (
        <section className="flex flex-col justify-around">
            <img src="logo.png" alt="logo" />
            <button>GET STARTED</button>
        </section>
    );
};

export default Home;
